# Image-Classification)
The purpose of this project is to classify given images into its classification labels, namely cat & dog. Instead of creating a model from scratch, I applied
a pre-trained model called "EfficientNet" to leverage its power.
